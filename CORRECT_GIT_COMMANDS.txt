# Correct Git Commands for AndreFerreira23
# Repository: https://github.com/AndreFerreira23/Fourfold-Fate

# Step 1: Navigate to project
cd "C:\Users\arset\Fourfold Fate"

# Step 2: Initialize git (if not already done)
git init

# Step 3: Add all files
git add .

# Step 4: Commit
git commit -m "Initial setup: Core systems, UI, and scene setup"

# Step 5: Check if remote already exists
git remote -v

# Step 6: If remote exists with wrong URL, remove it first:
# git remote remove origin

# Step 7: Add remote (correct URL)
git remote add origin https://github.com/AndreFerreira23/Fourfold-Fate.git

# Step 8: Push to GitHub
git branch -M main
git push -u origin main

# If you get authentication error, you may need to:
# 1. Use a Personal Access Token instead of password
# 2. Or use GitHub Desktop (easier!)

